/**
 * @deprecated Import from `@/config` instead.
 * Kept so older imports keep working during the template migration.
 */
export {
  wedding,
  weddingConfig,
  weddingData,
  galleryImages,
} from "./compat";

export type { WeddingContent } from "./compat";
